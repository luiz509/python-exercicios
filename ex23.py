# Entrada do usuário: base e altura do triângulo
base = float(input("Digite a base do triângulo: "))
altura = float(input("Digite a altura do triângulo: "))

# Cálculo da área do triângulo
area = (base * altura) / 2

# Saída do resultado
print("A área do triângulo é:", area)
